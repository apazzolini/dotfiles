fasd_cd() {
  if [ $1 ]; then
    local _fasd_ret="$(fasd -e 'printf %s' "$@")"
    [ -d "$_fasd_ret" ] && cd "$_fasd_ret" || printf %s\n "$_fasd_ret"
  else
    local _fasd_ret="$(fasd -d -l | fzf)"
    cd "$_fasd_ret"
  fi
}
