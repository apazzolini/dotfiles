return {
  -- 'catppuccin/nvim',
  -- priority = 10000,
  -- config = function()
  --   local catppuccin = require('catppuccin')

  --   catppuccin.setup({
  --     flavour = 'mocha',
  --     custom_highlights = function(colors)
  --       return {
  --         ['CmpItemMenu'] = {
  --           fg = colors.overlay0,
  --         },
  --       }
  --     end,
  --   })

  --   catppuccin.load()
  -- end,
}
